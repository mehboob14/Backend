//import {sum,multiply,PI} from "./export.js"
import { generate } from "random-words";
// this will throw an error because first we have to create package.json first 
//without package.json we are't use export and import
console.log(generate());
//console.log(PI);
//console.log(sum(2,3));
//console.log(multiply(2,3));
